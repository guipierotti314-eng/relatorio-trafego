# Base compartilhada

Adicione nesta pasta o arquivo real `base_atual.xlsx` com as abas obrigatórias
`dados-face` e `dados-google`. O dashboard carrega exclusivamente esse caminho.

O arquivo não foi criado automaticamente para evitar publicar dados fictícios.
